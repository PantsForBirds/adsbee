# Debug partition table — the same nvs/phy_init/factory layout as partitions_remote_id.csv (3 MB app, so the
# Bluetooth-enabled Remote ID build fits) plus a coredump partition at the end of flash.
# The image is built for 4 MB flash (it must run on the 4 MB ESP32-S3-MINI-1U-N4R2 as well as the 8 MB -N8), so the
# coredump is placed at the very end of the first 4 MB (0x400000 - 256K = 0x3C0000), clear of the app (ends 0x310000).
# Name,   Type, SubType,  Offset,    Size,   Flags
nvs,      data, nvs,      0x9000,    0x6000,
phy_init, data, phy,      0xF000,    0x1000,
factory,  app,  factory,  0x10000,   3M,
coredump, data, coredump, 0x3C0000,  256K,
